<?xml version="1.0" encoding="utf-8"?>
<Page x:ConnectionId='1'
    x:Class="Fluent_Launcher.Assets.Pages.Home.Page_SelectInstance"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluent_Launcher.Assets.Pages.Home"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls" 
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <ListView x:ConnectionId='2' Grid.Column="0"                                  SelectedIndex="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="0, 10, 16, 10">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="{Binding FolderName}" Style="{StaticResource SettingsCardHeaderTextStyle}"/>
                        <TextBlock Grid.Row="1" Text="{Binding FolderPath}" Style="{StaticResource SettingsCardDescriptionTextStyle}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Expander Grid.Column="1" Header="All Instances" MinWidth="500" VerticalAlignment="Top" Margin="0, 0, 10, 0" HorizontalAlignment="Stretch" IsExpanded="True">
            <ListBox x:ConnectionId='3' x:Name="ListBox_AllInstance"                                  Background="Transparent" SelectionMode="Single"                                                        >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Id}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Expander>
    </Grid>
</Page>

